# ISP Compare

<p align="center">
  <img src="https://img.shields.io/badge/status-diploma%20project-blue" alt="Project Status"/>
  <img src="https://img.shields.io/badge/python-3.13-blue" alt="Python Version"/>
  <img src="https://img.shields.io/badge/fastapi-0.115-blue" alt="FastAPI Version"/>
  <img src="https://img.shields.io/badge/react-19.1-blue" alt="React Version"/>
</p>

## 📝 Описание

ISP Compare - это веб-сервис, предназначенный для пользователей, которым необходим удобный инструмент для
сравнения и выбора интернет-провайдеров. С его помощью можно анализировать качество предоставляемых услуг, сравнивать
стоимость тарифных планов различных провайдеров и получать актуальную информацию о доступности услуг в Волгограде.

### Основные возможности:

- Просмотр и сравнение тарифов всех крупных интернет-провайдеров
- Фильтрация тарифов по цене, скорости и дополнительным услугам
- Подробное сравнение до 5 тарифов одновременно с аналитическими показателями
- Система отзывов о провайдерах
- История поиска и сохранение предпочтений
- Личный кабинет пользователя
- Административная панель для управления контентом

## 🛠️ Технический стек

### Backend

- Python 3.13
- FastAPI 0.115 - асинхронный веб-фреймворк
- SQLAlchemy 2.0 - ORM для работы с базой данных
- Dishka - DI фреймворк
- Pydantic - валидация данных и сериализация
- JWT - для авторизации пользователей
- Alembic - для миграций базы данных
- SQLAdmin - административная панель

### Frontend

- TypeScript
- React 19.1
- React Router 7.5
- Axios - HTTP клиент
- Vite - сборщик

### Инфраструктура

- PostgreSQL 17 - реляционная база данных
- Redis 7 - для хранения черного списка токенов и кеширования
- Docker - контейнеризация
- Docker Compose - оркестрация контейнеров
- Nginx - веб-сервер для продакшена

## 🗂️ Структура проекта

```
.
├── backend/                        # Серверная часть приложения
│   ├── src/                        # Исходный код
│   │   └── isp_compare/            # Основной модуль приложения
│   │       ├── admin/              # Административная панель
│   │       ├── api/                # API endpoints
│   │       │   └── v1/             # Версия API
│   │       ├── core/               # Конфигурация и ядро приложения
│   │       │   └── di/             # Настройки DI
│   │       ├── models/             # Модели базы данных
│   │       ├── repositories/       # Репозитории для доступа к данным
│   │       ├── schemas/            # Pydantic схемы для валидации данных
│   │       ├── services/           # Бизнес-логика
│   │       └── main.py             # Точка входа в приложение
│   ├── migrations/                 # Миграции базы данных (Alembic)
│   │   └── versions/               # Версии миграций
│   ├── scripts/                    # Скрипты для развертывания
│   ├── tests/                      # Тесты
│   ├── Dockerfile                  # Docker-конфигурация для production
│   ├── Dockerfile.dev              # Docker-конфигурация для разработки
│   ├── pyproject.toml              # Зависимости и метаданные проекта
│   └── poetry.lock                 # Зафиксированные версии зависимостей
│
├── frontend/                       # Клиентская часть приложения
│   ├── public/                     # Статические файлы
│   ├── src/                        # Исходный код
│   │   ├── components/             # React компоненты
│   │   ├── contexts/               # Контексты React
│   │   ├── hooks/                  # Пользовательские хуки
│   │   ├── services/               # Сервисы для работы с API
│   │   ├── types/                  # TypeScript типы и интерфейсы
│   │   ├── App.tsx                 # Основной компонент приложения
│   │   └── main.tsx                # Точка входа
│   ├── nginx/                      # Конфигурация Nginx для контейнера
│   ├── Dockerfile                  # Docker-конфигурация для production
│   ├── Dockerfile.dev              # Docker-конфигурация для разработки
│   ├── package.json                # Зависимости и скрипты
│   └── vite.config.ts              # Конфигурация Vite
│
├── nginx/                          # Конфигурация Nginx для продакшена
│   └── conf.d/                     # Директива конфигурации
│
├── docker-compose.dev.yml          # Конфигурация для разработки
├── docker-compose.prod.yml         # Конфигурация для продакшена
├── .env.example                    # Пример файла с переменными окружения
└── README.md                       # Этот файл
```

## 🚀 Установка и запуск

### Предварительные требования

- Docker и Docker Compose
- Git

### Шаги установки для разработки

1. Клонируйте репозиторий:

```bash
git clone https://github.com/bodaue/isp-compare.git
cd isp-compare
```

2. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

3. Отредактируйте файл `.env`, настроив необходимые переменные окружения

4. Запустите приложение в режиме разработки:

```bash
docker-compose -f docker-compose.dev.yml up --build
```

5. Приложение будет доступно по адресу:
    - Frontend: http://localhost:5173
    - Backend API: http://localhost:8000
    - API документация: http://localhost:8000/docs
    - Админ-панель: http://localhost:8000/admin

### Шаги установки для продакшена

1. Клонируйте репозиторий:

```bash
git clone https://github.com/bodaue/isp-compare.git
cd isp-compare
```

2. Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

3. Отредактируйте файл `.env` для продакшен окружения

4. Выпустите SSL сертификаты для вашего домена с помощью Let's Encrypt

5. Запустите приложение в продакшен режиме:

```bash
docker-compose -f docker-compose.prod.yml up -d --build
```

6. Приложение будет доступно по настроенному домену

## 🔒 Аутентификация

Система использует JWT для аутентификации:

1. Access token - короткоживущий токен для доступа к API
2. Refresh token - долгоживущий токен, хранящийся в HTTP-only cookie

Защищенные эндпоинты требуют заголовок `Authorization: Bearer <access_token>`.

Доступные операции:

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена
- `POST /api/auth/logout` - Выход

## 👨‍💻 Административная панель

Проект включает в себя административную панель, доступную по адресу `/admin`.
Для входа используйте учетные данные администратора:

- Логин: `admin`
- Пароль: определен в переменной окружения `INITIAL_ADMIN_PASSWORD` (по умолчанию "AdminPassword123")

В административной панели вы можете:

- Управлять пользователями
- Добавлять и редактировать провайдеров
- Управлять тарифами
- Модерировать отзывы

## 💡 Основные возможности

- **Просмотр провайдеров**: Список всех доступных провайдеров с базовой информацией
- **Детали провайдера**: Подробная информация о провайдере, его тарифах и отзывах
- **Сравнение тарифов**: Аналитическое сравнение до 5 тарифов с различными метриками
- **Фильтрация тарифов**: Поиск подходящих тарифов по цене, скорости и дополнительным услугам
- **Работа с отзывами**: Чтение и написание отзывов о провайдерах
- **Личный кабинет**: Управление профилем пользователя
- **История поиска**: Сохранение и восстановление предыдущих поисковых фильтров

## 📊 Особенности реализации

- **JWT с RefreshToken**: Механизм обновления токенов с сохранением безопасности
- **Rate Limiting**: Защита от перебора пароля и DDoS-атак
- **Асинхронная работа с БД**: Оптимизация производительности за счет асинхронных запросов
  - **Dockerized**: Полностью контейнеризированное приложение с возможностью запуска в любом окружении
- **HTTPS в продакшене**: Настроенный Nginx с поддержкой SSL для безопасного соединения